---
title: 'Login'
api: 'POST /api/login'
description: 'Authenticate a user and obtain an access token'
---

## Endpoint

```
POST /api/login
```

Authenticates a user with email and password credentials. Returns the user object with their empresa (business) relation if applicable, and a Sanctum authentication token.

## Authentication

No authentication required (public endpoint).

## Request Body

<ParamField body="email" type="string" required>
  User's email address.
</ParamField>

<ParamField body="password" type="string" required>
  User's password.
</ParamField>

## Response

<ResponseField name="status" type="string">
  Response status. Returns `success` on successful authentication.
</ResponseField>

<ResponseField name="user" type="object">
  The authenticated user object.
  
  <ResponseField name="user.id" type="integer">
    Unique user identifier.
  </ResponseField>
  
  <ResponseField name="user.name" type="string">
    User's full name.
  </ResponseField>
  
  <ResponseField name="user.email" type="string">
    User's email address.
  </ResponseField>
  
  <ResponseField name="user.rol" type="string">
    User's role. One of: `cliente`, `empresa`, or `admin`.
  </ResponseField>
</ResponseField>

<ResponseField name="token" type="string">
  Sanctum authentication token. Include this token in the `Authorization: Bearer {token}` header for all authenticated API requests.
</ResponseField>

<ResponseField name="redirectTo" type="string">
  Suggested redirect URL based on user role and profile completion:
  - `/empresa/create` for business users without a registered business
  - `/empresa/panel` for business users with a registered business
  - `/admin/dashboard` for administrators
  - `/` for customers
</ResponseField>

<ResponseField name="message" type="string">
  Success message.
</ResponseField>

## Example Request

```bash
curl -X POST https://api.beanquick.com/api/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "maria@example.com",
    "password": "SecurePass123"
  }'
```

## Example Response

### Customer Login

```json
{
  "status": "success",
  "user": {
    "id": 42,
    "name": "María García",
    "rol": "cliente",
    "email": "maria@example.com"
  },
  "token": "2|XyZ9AbCdEfGhIjKlMnOpQrStUvWxYz1234567890",
  "redirectTo": "/",
  "message": "Login exitoso"
}
```

### Business Owner Login (with empresa)

```json
{
  "status": "success",
  "user": {
    "id": 15,
    "name": "Carlos Rodríguez",
    "rol": "empresa",
    "email": "carlos@cafedelcentro.com"
  },
  "token": "3|AbC123XyZ456DefGhiJklMnoPqrStuVwxYz789",
  "redirectTo": "/empresa/panel",
  "message": "Login exitoso"
}
```

### Business Owner Login (without empresa)

```json
{
  "status": "success",
  "user": {
    "id": 28,
    "name": "Ana Martínez",
    "rol": "empresa",
    "email": "ana@example.com"
  },
  "token": "4|QwErTyUiOpAsDfGhJkLzXcVbNm1234567890",
  "redirectTo": "/empresa/create",
  "message": "Login exitoso"
}
```

## Error Responses

### Invalid Credentials (401)

```json
{
  "status": "error",
  "message": "Las credenciales no coinciden con nuestros registros."
}
```

### Validation Error (422)

```json
{
  "message": "The email field must be a valid email address.",
  "errors": {
    "email": [
      "The email field must be a valid email address."
    ]
  }
}
```

### Missing Fields (422)

```json
{
  "message": "The email field is required. (and 1 more error)",
  "errors": {
    "email": [
      "The email field is required."
    ],
    "password": [
      "The password field is required."
    ]
  }
}
```

## Notes

- The endpoint uses Laravel Sanctum for token-based authentication
- Tokens are named `token-auth` in the system
- For business users, the response logic checks if an `Empresa` record exists to determine the redirect path
- Store the returned token securely and include it in subsequent API requests
- The token should be sent as: `Authorization: Bearer {token}`
